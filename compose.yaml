version: '3.8'

services:
  ai-summarizer-telegram-bot:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: bot
    volumes:
      - ./:/app
    env_file:
      - path: ./.env
        required: true
    entrypoint: ["python", "main.py"]
    restart: always